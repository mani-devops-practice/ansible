- name: Install and run Nginx
  hosts: frontend
  become: yes
  tasks:
  - name: start Nginx
    ansible.builtin.package:
      name: nginx 
      state: present
  - name: enable Nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes